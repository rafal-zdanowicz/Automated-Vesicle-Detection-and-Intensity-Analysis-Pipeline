# Automated-Vesicle-Detection-and-Intensity-Analysis-Pipeline
A modular Python-based pipeline for automated detection, extraction, and quantitative analysis of fluorescence-labeled vesicles (e.g. liposomes). The workflow supports both pretrained YOLO models and new model training using manually annotated data. The scripts allow users to run the entire pipeline or individual components as needed.
